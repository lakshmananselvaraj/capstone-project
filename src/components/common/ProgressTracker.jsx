import React from "react";

const ProgressTracker = () => {
    return (
        <div>
            <h2>Progress Tracker</h2>
            <p>Track your learning progress here.</p>
        </div>
    );
};

export default ProgressTracker; // ✅ Ensure this line exists
